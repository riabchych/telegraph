<Window x:Class="Telegraph.TlgManagerWnd"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Telegraph"
        mc:Ignorable="d"
        Title="MainWindow" Height="526" Width="581" AllowDrop="True">
    <Grid>
        <ToolBarTray VerticalAlignment="Top" Height="40">
            <ToolBar Height="40">
                <Button Content="Add" />
                <Button Content="Edit" />
                <Button Content="Delete" />
            </ToolBar>
        </ToolBarTray>
        <Grid x:Name="rootGrid" Margin="0,40,0,25" VerticalAlignment="Stretch">
            <ListView DockPanel.Dock="Top" x:Name="telegramsList" ItemsSource="{Binding Telegrams}" VerticalContentAlignment="Center" BorderBrush="Khaki" DragLeave="telegramsList_DragLeave" Drop="telegramsList_Drop" DragEnter="telegramsList_DragEnter">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="50" Header="Number"  DisplayMemberBinding="{Binding Number}"  />
                        <GridViewColumn Width="70" Header="Date" DisplayMemberBinding="{Binding Date}" />
                        <GridViewColumn Width="120" Header="Subnum"  DisplayMemberBinding="{Binding Subnum}" />
                        <GridViewColumn Width="100" Header="Urgency"  DisplayMemberBinding="{Binding Urgency}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
        
        <StatusBar Height="25" VerticalAlignment="Bottom">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto" MinWidth="100"/>
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem>
                <TextBlock Name="StatusIndicator" Text="Ready"></TextBlock>
            </StatusBarItem>
            <StatusBarItem Grid.Column="1">
                <ProgressBar Width="100" Height="17" Name="ProgressBar" VerticalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" HorizontalAlignment="Right">
                    <ProgressBar.Background>
                        <LinearGradientBrush EndPoint="1,0" StartPoint="0,0">
                            <GradientStop Color="#FFBABABA" Offset="0"/>
                            <GradientStop Color="#FFC7C7C7" Offset="0.5"/>
                            <GradientStop Color="#FFBABABA" Offset="1"/>
                        </LinearGradientBrush>
                    </ProgressBar.Background>
                </ProgressBar>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
